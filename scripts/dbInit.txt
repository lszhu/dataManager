// 数据库维护备忘
// example for administrator
use admin

show users

db.createUser(
	{
		user: 'admin',
		pwd: 'letmein',
		roles: ['root']
	}
);

// example for each county

use chequesys

show users

db.createUser(
	{
		user: 'chequesys',
		pwd: 'chequeSys2014',
		roles: ['readWrite']
	}
);

db.project.ensureIndex({name: 1}, {unique: true});

db.project.ensureIndex({id: 1});
